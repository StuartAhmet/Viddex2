<div style="margin-top:10px">
    <ul class="nav nav-tabs" style="margin-bottom:20px">
      <li class="nav-item">
        <%=link_to user_backgrounds_path(@user, @backgrounds) do %>
          <button class="btn nav-link <%= "active" if current_page?(user_backgrounds_path) %> br5">
            <i class="bi bi-bounding-box mr10"></i>All Backgrounds
          </button>
        <% end %>
      </li>
      <li class="nav-item">
        <%=link_to new_user_background_path(@user, @background) do %>
          <button class="btn nav-link <%= "active" if current_page?(new_user_background_path) %> br5">
            <i class="bi bi-cloud-plus mr10"></i>Upload
          </button>
        <% end %>
      </li> <br>
    </ul>
</div>
