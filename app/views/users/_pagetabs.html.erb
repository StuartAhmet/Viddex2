<div style="margin-top:10px">
    <ul class="nav nav-tabs" style="margin-bottom:20px">
      <li class="nav-item">
        <%=link_to user_path(@user) do %>
          <button class="btn nav-link <%= "active" if current_page?(user_path) %> br5">
            <i class="bi bi-bounding-box mr10"></i>Usage Details
          </button>
        <% end %>
      </li>
      <li class="nav-item">
        <%=link_to edit_user_path(@user) do %>
          <button class="btn nav-link <%= "active" if current_page?(edit_user_path) %> br5">
            <i class="bi bi-cloud-plus mr10"></i>Personal Details
          </button>
        <% end %>
      </li>
    </ul>
</div>
